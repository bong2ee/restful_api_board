<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bong.board.repository.BoardRepository">

	<sql id="paging_prefix">
		<![CDATA[
		SELECT
		LAB_TABLE.*
		FROM (
		        SELECT
			    IN_TABLE.*
		        ,ROWNUM AS RN
			    FROM (
		]]>
	</sql>
	
	<sql id="paging_suffix">
		<![CDATA[
				) IN_TABLE
				WHERE ROWNUM <= ${pageIndex*rowcount+rowcount}
		) LAB_TABLE
		WHERE ${pageIndex*rowcount} < RN
		]]>
	</sql>
</mapper>

